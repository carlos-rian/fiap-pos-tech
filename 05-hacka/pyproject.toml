[project]
name = "05-hacka"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.0",
]

[dependency-groups]
dev = [
    "isort>=6.0.1",
    "ruff>=0.11.13",
]
